#include <iostream>
using namespace std;
int a;
int main()
{
	setlocale(LC_ALL, "Russian");
	cout << "Введите 3-х значное число" << endl;
	cin >> a;
	if    (  ((a/100>(a%100)/10) && (((a % 100) / 10)>a%10)) || ((a / 100 < (a % 100) / 10) && (((a % 100) / 10) < a % 10)))
	{
		cout << "Цифры данного числа находятся в возр. или убыв. последовательности" << endl;
	}
	else
	{
		cout << "Цифры данного числа не находятся в возр. или убыв. последовательности" << endl;
	}
	
	return 0;
}